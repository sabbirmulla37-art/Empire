# Empire Prime Bot

A feature-rich Discord bot built with discord.py.

## Setup
1. Install requirements: `pip install discord.py`
2. Replace "YOUR_TOKEN" in `bot.py` with your actual Discord Bot Token.
3. Run the bot: `python bot.py`
4. 
